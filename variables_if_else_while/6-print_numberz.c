#include <stdio.h>

/**
 * main - main block
 * Description: This program print the lowercase alphabet in reverse
 * Return: Always 0 (Success)
 */

int main(void)
{
	int abc;
		for (abc = 48; abc <= 57; abc++)
		{
			putchar(abc);
		}
	putchar('\n');
	return (0);
}

